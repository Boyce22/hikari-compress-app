export interface RecommendedProfile {
  codec: string;
  encoder: string;
  crf: number;
  preset: string;
  hardwareAcceleration: boolean;
  rationale: string;
  recommendedRam: number;
  fps: number;
  resolution: string;
}
